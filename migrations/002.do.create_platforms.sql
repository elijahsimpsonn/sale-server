CREATE TABLE IF NOT EXISTS platforms(
    user_id INTEGER NOT NULL REFERENCES users(user_id),
    platform_id INTEGER PRIMARY KEY GENERATED BY DEFAULT AS IDENTITY,
    platform_name VARCHAR NOT NULL
);